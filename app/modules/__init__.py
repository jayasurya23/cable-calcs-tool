"""Feature modules. Each module owns its routes, templates, and logic.

Current:
    sam_report  — SAM analysis upload -> report + CAD-bound table.

Planned (as the site grows):
    cable_calcs — the full DC/AC/MV cable sizing & voltage-drop suite.
"""
